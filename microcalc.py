import pandas as pd

# ===== NOT implemented yet =========
def compute_micro(
    df: pd.DataFrame,
    account_col_name: str,
    micro_formula: str,
    fields_sep: str,
):
    result = 1
    error = None
    return (result, error)
